## What this changes

<!-- One or two sentences. What is different for someone using the app? -->

## Why

<!-- The reason, especially if the change looks odd. If the obvious approach was
     tried and didn't work, say so here and in a code comment. -->

## Checks

- [ ] `swift run flowbar-tests` passes
- [ ] Behaviour that could be a rule lives in `FlowBarCore` with a test
- [ ] `CHANGELOG.md` updated, if a user would notice
- [ ] Built and used it (`./build-app.sh --run`), if it touches the UI
